<ul class="selected-list">
    @foreach (var item in ItemsSelecionados)
    {
        <button type="button" class="unset" @onclick="async (MouseEventArgs args) => await AoRemoverItemSelecionado(args, item)">
            <li class="selected-item">@(item)</li>
        </button>
    }
</ul>